meta:
  environment: node
  toolchain:
    name: npm
    version: 18

build:
  artifacts:
    "server/**/*": "/".

run:
  scriptName: server/src/server.js
  containerPort: 80
  command: npm run start
